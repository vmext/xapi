{
  "version": 2,
  "installCommand": "yarn install && yarn build",
  "buildCommandx": "yarn build",
  "builds": [
    {
      "src": "dist/vercel/vercel.cjs",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "dist/vercel/vercel.cjs"
    }
  ]
}
